[build]
target = "index.html"
dist   = "dist"

[watch]
watch = ["src", "index.html", "Cargo.toml", "Trunk.toml"]

[serve]
addresses = ["127.0.0.1"]
port     = 8080
open     = false

[clean]
dist = "dist"
